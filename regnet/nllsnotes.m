% for SNR 900 (ND, B) net 
% 1st sing vector parameters: 
% 1. Noisy: expcurve(t, 0.18, -0.0055, -0.05, 0.7, -0.3, min(v_nd));
% 2. Reg: expcurve(t, 0.18, -0.0055, -0.05, 0.7, -0.3, min(v_nd));
%
% 2nd sing vector parameters: 
% 1. Noisy: expcurve(t, 0.18, -0.0055, -0.05, 0.7, -0.3, min(v_nd));
% 2. Reg: expcurve(t, 0.18, -0.0055, -0.05, 0.7, -0.3, min(v_nd)); 
%    For this case, make sure to flip the data: solve  find_exp(-v_b, t)
%
% 3rd sing vector parameters: 
% 1. Noisy: expcurve(t, 0.18, -0.0055, -0.05, 0.7, -0.3, min(v_nd));
% 2. Reg: expcurve(t, 0.18, -0.0055, -0.05, 0.7, -0.3, min(v_nd)); 
%    For this case, make sure to flip the data: solve  find_exp(-v_b, t)
%
%
%